export const useStyle = () => {
  return {
    regularChartWrapper: {
      justifyContent: 'center',
      alignItems: 'center',
      width: '100%',
      height: 500,
    },
    smallChartWrapper: {
      justifyContent: 'center',
      alignItems: 'center',
      width: '100%',
      height: 300,
    },
  };
};
